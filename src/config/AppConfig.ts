export const AppConfig = {
  storageApiUrl: 'https://api-storage.arkturian.com',
  media: {
    useTrimmedImages: false,
  },
};

// Named export for compatibility
export const APP_CONFIG = AppConfig;
